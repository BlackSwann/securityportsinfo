<%= form_for(@operative_system) do |f| %>
  <% if @operative_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operative_system.errors.count, "error") %> prohibited this operative_system from being saved:</h2>

      <ul>
      <% @operative_system.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
