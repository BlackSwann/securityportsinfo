<%= form_for(@port_service) do |f| %>
  <% if @port_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@port_service.errors.count, "error") %> prohibited this port_service from being saved:</h2>

      <ul>
      <% @port_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :port_id %><br>
    <%= f.number_field :port_id %>
  </div>
  <div class="field">
    <%= f.label :service_id %><br>
    <%= f.number_field :service_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
