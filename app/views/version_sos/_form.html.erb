<%= form_for(@version_so) do |f| %>
  <% if @version_so.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@version_so.errors.count, "error") %> prohibited this version_so from being saved:</h2>

      <ul>
      <% @version_so.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
