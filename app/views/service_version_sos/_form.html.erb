<%= form_for(@service_version_so) do |f| %>
  <% if @service_version_so.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_version_so.errors.count, "error") %> prohibited this service_version_so from being saved:</h2>

      <ul>
      <% @service_version_so.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :version_so_id %><br>
    <%= f.number_field :version_so_id %>
  </div>
  <div class="field">
    <%= f.label :service_id %><br>
    <%= f.number_field :service_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
